#!/usr/bin/env bash
#
if [ $# -lt 2 ]; then
    echo "Usage: $0 <secret name> <field name>"
    exit 1
fi

SECRET_NAME=$1
FIELD_NAME=$2

echo "$SECRET_NAME"

kubectl get secret "$SECRET_NAME" -o jsonpath='{.data}' | jq -r '.'"$FIELD_NAME" | base64 --decode

